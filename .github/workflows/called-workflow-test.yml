on:
  workflow_call:
    inputs:
      translation_directory:
        description: directory to be passed to pybabel compile --directory=...
        required: false
        type: string

jobs:
  build-n-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: 3.9
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -U setuptools wheel babel
      - name: compile .po files
        run: |
          if [[ -f setup.cfg && grep -q "compile_catalog" setup.cfg ]]
          then
            python setup.py compile_catalog
          elif [[ -n "${{inputs.translation_directory}}" ]]
            pybabel compile --directory=${{inputs.translation_directory}} --use_fuzzy  # TODO: sanitize input
          fi
      - name: check for compilation
        run: |
          find . -name "*.mo"
